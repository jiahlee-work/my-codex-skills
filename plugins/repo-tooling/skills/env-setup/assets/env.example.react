# Environment template. Do not put real secrets in this file.
# Variables prefixed with VITE_ are exposed to the browser.

VITE_APP_URL=http://localhost:5173

# Do not put server secrets in a browser app.
# API_BASE_URL=
