# Local development only. Do not commit real secrets.
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.

NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server-only secrets. Replace locally.
# DATABASE_URL=
# OPENAI_API_KEY=
