# Normalized Ticket

All ticket sources produce the shared type in `shared/types/ticket.ts`.

Required fields:

- `key`, `title`, `workType`, `summary`
- `acceptanceCriteria`
- `source`

Optional metadata includes priority, status, space, assignee, reporter,
description, notes, comments, blockers, and raw source data.
